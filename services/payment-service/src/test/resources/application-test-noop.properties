# Intentionally empty — exists only so spring.config.import has a valid,
# resolvable target that replaces the production "optional:sm://" Secret Manager
# import without triggering Assert.hasText on a blank value.
