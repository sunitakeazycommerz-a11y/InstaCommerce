{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "StockReleased",
  "type": "object",
  "required": ["reservationId", "orderId", "releasedAt"],
  "properties": {
    "reservationId": { "type": "string", "format": "uuid" },
    "orderId": { "type": "string", "format": "uuid" },
    "releasedAt": { "type": "string", "format": "date-time" }
  }
}
