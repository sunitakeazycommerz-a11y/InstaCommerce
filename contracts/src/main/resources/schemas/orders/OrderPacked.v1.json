{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "OrderPacked",
  "type": "object",
  "required": ["orderId", "userId", "packedAt", "storeId"],
  "properties": {
    "orderId": { "type": "string", "format": "uuid" },
    "userId": { "type": "string", "format": "uuid" },
    "storeId": { "type": "string" },
    "packedAt": { "type": "string", "format": "date-time" }
  }
}
